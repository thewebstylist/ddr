{
  "name": "loft",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Loft — an infinite design canvas with real project boards on it.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b --noCheck && vite build",
    "typecheck": "tsc -b",
    "preview": "vite preview",
    "bundle": "npm run build && node scripts/inline.mjs"
  },
  "dependencies": {
    "immer": "^10.1.1",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0"
  }
}
